'use client';
import {useEffect,useRef,useState} from 'react';import {useRouter} from 'next/navigation';import {createClient} from '@/lib/supabase/client';
type Opt={id:string;option_key:string;option_text:string;is_correct:boolean;rationale:string|null;display_order:number};type Q={id:string;version:number;stem:string;exam_tracks:string[];subject:string;topic:string;client_need:string;clinical_judgment_step:string|null;difficulty:string;rationale_correct:string;rationale_distractors:string;memory_rule:string|null;question_options:Opt[]};
export default function Practice(){const r=useRouter(),start=useRef(Date.now());const [track,setTrack]=useState('RN'),[qs,setQs]=useState<Q[]>([]),[i,setI]=useState(0),[sel,setSel]=useState<number|null>(null),[sub,setSub]=useState(false),[saving,setSaving]=useState(false),[loading,setLoading]=useState(true),[err,setErr]=useState(''),[score,setScore]=useState(0),[finished,setFinished]=useState(false);const q=qs[i];
useEffect(()=>{(async()=>{const s=createClient();const {data:{user}}=await s.auth.getUser();if(!user){r.replace('/auth?track=rn');return}const {data:p}=await s.from('profiles').select('exam_track').eq('id',user.id).maybeSingle();const t=p?.exam_track==='pn'?'pn':'rn';setTrack(t.toUpperCase());const {data,error}=await s.from('question_versions').select('question_id,version,stem,exam_tracks,subject,topic,client_need,clinical_judgment_step,difficulty,rationale_correct,rationale_distractors,memory_rule,question_options(id,option_key,option_text,is_correct,rationale,display_order)').eq('topic','Heart Failure').contains('exam_tracks',[t]);if(error){setErr(error.message);setLoading(false);return}const rows=(data||[]).map((x:any)=>({id:x.question_id,...x,question_options:[...(x.question_options||[])].sort((a:Opt,b:Opt)=>a.display_order-b.display_order)}));setQs(rows);setLoading(false)})()},[r]);
async function submit(){if(sel===null||sub||!q)return;setSaving(true);setErr('');const s=createClient();const {data:{user}}=await s.auth.getUser();if(!user)return;const option=q.question_options[sel],ok=option.is_correct;const {error}=await s.from('question_attempts').insert({user_id:user.id,question_id:q.id,question_version:q.version,selected_answer:{option:option.option_key,text:option.option_text},is_correct:ok,response_time_ms:Date.now()-start.current,client_need:q.client_need,clinical_judgment_step:q.clinical_judgment_step,subject:q.subject,difficulty:q.difficulty});setSaving(false);if(error){setErr(error.message);return}if(ok)setScore(x=>x+1);setSub(true)}function next(){if(i===qs.length-1){setFinished(true);return}setI(x=>x+1);setSel(null);setSub(false);setErr('');start.current=Date.now()}
if(loading)return <main className="grid min-h-screen place-items-center bg-slate-50"><p className="font-bold text-slate-500">Loading your NCLEX practice…</p></main>;if(!q&&!finished)return <main className="grid min-h-screen place-items-center bg-slate-50 px-6"><section className="max-w-lg rounded-2xl bg-white p-8 text-center shadow"><h1 className="text-2xl font-black text-[var(--deep-navy)]">Practice content needs one setup step</h1><p className="mt-3 text-slate-600">{err||'No active Heart Failure questions were found for this track. Run migration 0002_question_engine.sql in Supabase, then refresh this page.'}</p><a href="/dashboard" className="mt-6 inline-block rounded-xl bg-[var(--blue)] px-6 py-3 font-black text-white">Back to Dashboard</a></section></main>;
if(finished)return <main className="grid min-h-screen place-items-center bg-slate-50 px-6"><section className="max-w-xl rounded-[2rem] bg-white p-9 text-center shadow-xl ring-1 ring-slate-200"><p className="font-black text-[var(--teal)]">PRACTICE COMPLETE</p><h1 className="mt-3 text-4xl font-black text-[var(--deep-navy)]">{score} of {qs.length} correct</h1><p className="mt-4 leading-7 text-slate-600">Your responses were saved to your performance history. These results now feed your dashboard.</p><button onClick={()=>r.push('/dashboard')} className="mt-7 w-full rounded-xl bg-[var(--blue)] py-4 font-black text-white">View Updated Dashboard →</button></section></main>;
const correct=q.question_options.find(x=>x.is_correct);return <main className="min-h-screen bg-slate-50"><header className="bg-[var(--deep-navy)] text-white"><div className="mx-auto flex max-w-5xl justify-between px-6 py-5"><a href="/dashboard" className="text-xl font-black">NursePrep<span className="text-[var(--aqua)]">IQ</span></a><b>NCLEX-{track} Practice</b></div></header><div className="mx-auto max-w-3xl px-6 py-9"><div className="mb-4 flex justify-between"><span className="rounded-full bg-cyan-100 px-3 py-1 text-xs font-black text-[var(--teal)]">{(q.clinical_judgment_step||'NCLEX PRACTICE').toUpperCase()}</span><span className="text-sm font-bold text-slate-500">{q.topic} • {i+1} of {qs.length}</span></div><div className="mb-6 h-2 overflow-hidden rounded-full bg-slate-200"><div className="h-full bg-[var(--teal)]" style={{width:`${((i+(sub?1:0))/qs.length)*100}%`}}/></div><section className="rounded-[2rem] bg-white p-7 shadow-sm ring-1 ring-slate-200"><p className="text-sm font-bold text-slate-500">Single best answer • {q.difficulty}</p><h1 className="mt-3 text-xl font-black leading-8 text-[var(--deep-navy)]">{q.stem}</h1><div className="mt-6 space-y-3">{q.question_options.map((x,n)=><button key={x.id} disabled={sub} onClick={()=>setSel(n)} className={`flex w-full gap-3 rounded-2xl border p-4 text-left font-semibold ${sub&&x.is_correct?'border-emerald-500 bg-emerald-50':sub&&n===sel&&!x.is_correct?'border-rose-400 bg-rose-50':sel===n?'border-[var(--blue)] bg-blue-50':'border-slate-200'}`}><span className="font-black uppercase">{x.option_key}</span>{x.option_text}</button>)}</div>{err&&<p className="mt-4 rounded-xl bg-rose-50 p-3 font-bold text-rose-800">Could not save: {err}</p>}{!sub&&<button onClick={submit} disabled={sel===null||saving} className="mt-6 w-full rounded-xl bg-[var(--blue)] py-4 font-black text-white disabled:opacity-40">{saving?'Saving answer…':'Submit Answer →'}</button>}</section>{sub&&<section className="mt-6 space-y-4 rounded-[2rem] bg-white p-7 shadow-sm ring-1 ring-slate-200"><div className={`rounded-2xl p-4 font-black ${q.question_options[sel!].is_correct?'bg-emerald-50':'bg-amber-50'}`}>{q.question_options[sel!].is_correct?'✓ Correct — now lock in why.':'Not quite — use the rationale to strengthen the pattern.'}</div><div><b className="text-sm text-[var(--teal)]">WHY THE CORRECT ANSWER WORKS</b><p className="mt-1 leading-7 text-slate-600">{q.rationale_correct}</p></div><div><b className="text-sm text-[var(--teal)]">WHY THE OTHER ANSWERS ARE WEAKER</b><p className="mt-1 leading-7 text-slate-600">{q.rationale_distractors}</p></div>{correct?.rationale&&<div><b className="text-sm text-[var(--teal)]">KEY ANSWER INSIGHT</b><p className="mt-1 leading-7 text-slate-600">{correct.rationale}</p></div>}{q.memory_rule&&<div className="rounded-2xl bg-cyan-50 p-5"><b>Memory rule</b><p className="mt-1 font-semibold">{q.memory_rule}</p></div>}<button onClick={next} className="w-full rounded-xl bg-[var(--blue)] py-3.5 font-black text-white">{i===qs.length-1?'See My Results →':'Next Question →'}</button></section>}</div></main>}
