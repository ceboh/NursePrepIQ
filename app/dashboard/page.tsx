'use client';

import { useEffect, useState } from 'react';
import { createClient } from '@/lib/supabase/client';

export default function Dashboard(){
 const [email,setEmail]=useState('Student'); const [track,setTrack]=useState('RN');
 useEffect(()=>{ const supabase=createClient(); supabase.auth.getUser().then(({data})=>{if(data.user){setEmail(data.user.email?.split('@')[0]||'Student'); const t=(data.user.user_metadata?.track||'rn').toUpperCase(); setTrack(t);}}); },[]);
 const cards=[['Readiness','Building','Complete your diagnostic to establish a baseline.'],['Questions','0','Your completed practice questions will appear here.'],['Accuracy','—','Accuracy begins after your first practice session.'],['Study streak','0 days','Small, consistent sessions beat cramming.']];
 return <main className="min-h-screen bg-slate-50"><header className="bg-[var(--deep-navy)] text-white"><div className="mx-auto flex max-w-7xl items-center justify-between px-6 py-5 lg:px-8"><a href="/" className="text-xl font-black">NursePrep<span className="text-[var(--aqua)]">IQ</span></a><div className="flex items-center gap-4 text-sm font-bold"><span>{track} Prep</span><button onClick={async()=>{await createClient().auth.signOut(); location.href='/';}} className="rounded-lg border border-white/20 px-3 py-2">Sign out</button></div></div></header><section className="dashboard-hero"><div className="mx-auto max-w-7xl px-6 py-12 lg:px-8"><p className="font-bold text-cyan-200">YOUR {track} PREP DASHBOARD</p><h1 className="mt-2 text-4xl font-black text-white">Welcome, {email}.</h1><p className="mt-3 max-w-2xl text-slate-200">Your job today is simple: learn one thing well, practice it, and understand every mistake.</p></div></section><div className="mx-auto max-w-7xl px-6 py-10 lg:px-8"><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{cards.map(([a,b,c])=><div key={a} className="rounded-2xl bg-white p-5 shadow-sm ring-1 ring-slate-200"><p className="text-sm font-bold text-slate-500">{a}</p><p className="mt-2 text-2xl font-black text-[var(--deep-navy)]">{b}</p><p className="mt-2 text-sm leading-5 text-slate-500">{c}</p></div>)}</div><div className="mt-7 grid gap-6 lg:grid-cols-[1.3fr_.7fr]"><div className="rounded-[2rem] bg-white p-7 shadow-sm ring-1 ring-slate-200"><div className="flex items-center justify-between"><div><p className="text-sm font-black text-[var(--teal)]">TODAY’S PLAN</p><h2 className="mt-1 text-2xl font-black text-[var(--deep-navy)]">Build your clinical reasoning</h2></div><span className="rounded-full bg-cyan-50 px-3 py-1 text-xs font-black text-[var(--teal)]">~25 MIN</span></div><div className="mt-6 space-y-3">{['Learn: Heart Failure Made Simple','Practice: 10 priority questions','Clinical Judgment: Recognize Cues'].map((x,i)=><div key={x} className="flex items-center gap-4 rounded-2xl bg-slate-50 p-4"><span className="grid h-9 w-9 place-items-center rounded-xl bg-[var(--deep-navy)] font-black text-[var(--aqua)]">{i+1}</span><span className="font-bold">{x}</span></div>)}</div><button className="mt-5 w-full rounded-xl bg-[var(--blue)] py-3.5 font-extrabold text-white">Start Today’s Session →</button></div><div className="rounded-[2rem] bg-[var(--deep-navy)] p-7 text-white shadow-lg"><p className="text-sm font-black text-[var(--aqua)]">FIRST MILESTONE</p><h2 className="mt-2 text-2xl font-black">Establish your baseline.</h2><p className="mt-3 leading-7 text-slate-200">A short diagnostic will help NursePrepIQ identify where to focus first.</p><button className="mt-6 w-full rounded-xl bg-[var(--aqua)] py-3 font-extrabold text-[var(--deep-navy)]">Take Diagnostic →</button></div></div></div></main>;
}
